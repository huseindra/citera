"""Citera pipeline library.

A library, not a service: imported by the API (and later by workers).
Populated milestone by milestone — ingest (M2), retrieve (M3), findings (M4).
"""

__version__ = "0.1.0"
